{
  "name": "zeitspiegel-uitest",
  "private": true,
  "version": "0.0.0",
  "description": "Headless-browser unit tests for the control page (docs/TESTPLAN.md §1.1). Test-only: the page itself ships with no build tooling and no dependencies (ARCHITECTURE §D5).",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
